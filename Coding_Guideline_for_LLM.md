# コーディングガイドライン（LLM 向け）

## UI デザインガイドライン

本アプリケーションは、特許という堅牢な情報を扱いながらも、AI による創造的な示唆を与えるツールです。
「信頼性（Trust）」と「先進性（Innovation）」を両立するデザインシステムを採用します。

### 1. 基本スタンス (Core Principles)

-   **Clean Professional**: 情報量は多いが、余白と区切り線を適切に用い、圧迫感を与えない。
-   **Traceable UI**: AI の回答と情報源（特許原文）の繋がりを視覚的に明確にする。
-   **Motion for Wait**: 生成待ち時間を「処理中」のポジティブな体験に変えるアニメーションを採用する。

### 2. 基本スタイル (Foundation)

-   **フレームワーク**: Tailwind CSS
-   **アイコン**: Lucide React
    -   基本サイズ: `size={20}` (ボタン・メニュー), `size={16}` (補足情報), `size={24}` (強調・ロゴ)
-   **フォント**: `Noto Sans JP` (Google Fonts)
    -   ウェイト: 本文 `400`, 強調 `500`, 見出し `700`

### 3. カラーパレット (Color System)

ベースの `Slate` に加え、機能ごとに意味を持たせた 3 つのメインカラーを使い分けます。

**A. ベースカラー (Structure)**

画面の骨格となる無彩色です。

-   **背景**: `bg-slate-50` (アプリ背景), `bg-white` (コンテナ・カード)
-   **テキスト**: `text-slate-800` (主要テキスト), `text-slate-500` (サブテキスト), `text-slate-400` (プレースホルダー・メタ情報)
-   **ボーダー**: `border-slate-200` (基本), `border-slate-300` (入力フォーム等)

**B. 機能別アクセント (Functional Colors)**

| **色系統**  | **Tailwind Class** | **用途・意味**        | **実装例**                                                                              |
| ----------- | ------------------ | --------------------- | --------------------------------------------------------------------------------------- |
| **Blue**    | `blue-600`         | **プライマリ / 操作** | 信頼、確定、リンク、アップロード、 送信ボタン、選択状態、リンクテキスト                 |
| **Indigo**  | `indigo-600`       | **AI 解析 / 知性**    | AI による思考、解析プロセス、ボット発言、 AI、 アイコン背景(`indigo-100`)、ローディング |
| **Emerald** | `emerald-600`      | **アイデア / 成果**   | 生成されたアイデア、成功、完了、アイデアセクション、完了バッジ、生成ボタン              |

**C. 状態色 (State)**

-   **Error**: `red-500` / `bg-red-50` (エラーメッセージ)
-   **Highlight**: `yellow-100` (原文参照時のハイライト演出)

## 4. レイアウト構造 (Layout)

**ナビゲーション (Sidebar)**

-   **背景**: `bg-slate-900`
-   **文字**: `text-slate-400` (通常), `text-white` (ホバー/アクティブ)
-   **アクティブ表現**: `bg-blue-600` + **右端にアクセント線** (`border-r-4 border-blue-300`)
-   **レスポンシブ**: モバイル時はオーバーレイ表示 (`backdrop-blur-sm`)

**右サイドバー (Source Sidebar)**

-   **用途**: 特許原文や図面の参照用
-   **スタイル**: 白背景、左ボーダー (`border-l`), 幅固定 (`w-105`), 画面右端に固定 (`fixed right-0`)
-   **挙動**: 原文参照ボタンクリックでスライドイン (`translate-x-0`)

## 5. コンポーネントパターン (Components)

**カード・コンテナ (Cards)**

-   **基本**: `bg-white rounded-xl shadow-sm border border-slate-200`
-   **アイデアカード**: `ring-1 ring-emerald-50 border-emerald-100 shadow-lg` (アイデア生成結果の強調)

**ボタン (Buttons)**

-   **Primary**: `bg-blue-600 text-white hover:bg-blue-700 rounded-lg shadow-sm`
-   **Secondary**: `bg-white text-slate-600 border border-slate-300 hover:bg-slate-50`
-   **Action (Idea)**: `bg-emerald-600 text-white hover:bg-emerald-700` (解析からアイデア生成へ進む際など)

### 生成テキスト (Markdown Output)

AI 出力（Markdown）は `markdown_style.css` に基づき整形します。

-   **見出し**: 太字 (`font-bold`) + マージン確保
-   **リスト**: インデント確保 (`padding-left`) + マーカー色調整
-   **引用/参照**:
    -   通常: `text-slate-400 text-xs` (`[段落: 0013]`など)
    -   インタラクティブ: `hover:text-blue-700 hover:underline cursor-pointer` (クリック可能な場合)

### アニメーション (Micro Interactions)

ユーザーを待たせないための演出を積極的に採用します。

-   **出現**: `animate-in fade-in slide-in-from-bottom-4 duration-500` (生成結果の表示)
-   **待機**: `animate-pulse` (生成中のカーソル点滅、ローディングテキスト)
-   **回転**: `animate-spin` (ローディングアイコン)

---

## コーディング規約とコメントのガイドライン

コードを出力する際は、以下のコメントスタイルに則ってください。
また、以下のガイドラインに限らず、私のコードベースから観察できるスタイルを参考にしてください。

### 基本原則

-   視認性（Visibility）
-   教育的（Educational）

### 1. **セクションの明示**:

処理の塊ごとに、以下のような装飾コメントで区切りを入れてください。

```text
# ============================================================
# [処理名]
# ============================================================
```

### 2. **「なぜ」と「具体例」を書く**:

単に処理内容（What）を書くのではなく、「なぜそうするのか（Why）」や可能であれば「どうなるのか（Example）」を書いてください。

### 3. **正規表現や複雑なロジック**:

必ず「変換前」と「変換後」の具体例をコメントに併記してください。

### 4. **省略禁止**:

変数名や関数名は省略しない。（悪い例: data --> d）
「// ... existing code」のような省略はせず、コメントも含めて丁寧に記述してください。（※ ただし、ファイル全体ではなく一部分の修正であれば該当部分のみで構いません。）
